# HISTORICAL REFERENCE - This file documents dependencies used during model training.
# The repository no longer supports local model training.
# 
# For running the detection app, use: websitedetectiontest/requirements.txt
#
# The dependencies below are preserved for reference only and should not be installed
# unless you have a backup of the Mendeley training dataset and wish to develop the training pipeline locally.

# Training-only dependencies (archived for historical reference):
tensorflow==2.15.0
transformers==4.36.0
torch==2.2.0
beautifulsoup4==4.12.2
lxml==4.9.3
numpy==1.24.3
pandas==2.1.3
scikit-learn==1.3.2
matplotlib==3.8.2
seaborn==0.13.0
jupyter==1.0.0
pytest==7.4.3
tqdm==4.66.1
langdetect==1.0.9
deepl==1.17.0
python-dotenv==1.0.0
